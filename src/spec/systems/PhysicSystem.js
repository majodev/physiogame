define(["systems/PhysicSystem"],
  function(PhysicSystem) {
    describe("PhysicSystem", function() {

      it("test", function() {
        
      //   var system = new PhysicSystem("physys");
      //   expect(system).to.be.an.instanceof(PhysicSystem);

      });

    });
  }
);